# We The Party Platform

Big Things Coming

## Setup

Link the `hooks` directory to your git directory

```
ln -s ${full-project-path}/hooks ${full-project-path}/.git
```

## TODO

- Figure out how to get the githooks to set env vars
